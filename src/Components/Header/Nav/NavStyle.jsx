import styled from "styled-components";
import {FlexboxSpaceAround} from "../../../utils"

export const Ul = styled.ul`
${FlexboxSpaceAround};    
`

export const Li = styled.li`
    list-style-type: none;
    padding: 0;
`
